---
- hosts: localhost
  connection: local
  gather_facts: yes

  tasks:
    - name: Actualizar cache de APT
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Instalar herramientas de sistema y diseño
      apt:
        name:
          - figlet
        state: present
